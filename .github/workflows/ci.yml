    # Step 5: Dynamically locate and inject mock secrets regardless of folder casing
    - name: Inject Mock Streamlit Secrets
      run: |
        mkdir -p .streamlit
        
        # Use find to locate the file safely regardless of whether the folder is named 'tests' or 'Tests'
        MOCK_PATH=$(find . -iname "mock_secrets.toml" -print -quit)
        
        if [ -n "$MOCK_PATH" ]; then
          echo "Found mock secrets at: $MOCK_PATH"
          cp "$MOCK_PATH" .streamlit/secrets.toml
        else
          echo "ERROR: mock_secrets.toml could not be found anywhere in the repository layout!"
          echo "Printing repository directory contents for debugging:"
          ls -R
          exit 1
        fi      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    # Run the testing suite framework
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-mock
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run pytest validation matrix
      run: |
        python -m pytest tests/ --verbose    # Run the testing suite framework
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-mock
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run pytest validation matrix
      run: |
        python -m pytest tests/ --verbose        python -m pip install --upgrade pip
        pip install pytest pytest-mock
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

        # Step 5: Inject mock secrets using absolute workspace paths
     Tar name: Inject Mock Streamlit Secrets
      run: |
        mkdir -p $GITHUB_WORKSPACE/.streamlit
        cp $GITHUB_WORKSPACE/tests/mock_secrets.toml $GITHUB_WORKSPACE/.streamlit/secrets.toml

    # Step 6: Execute the test suite
    - name: Run pytest validation matrix
      run: |
        python -m pytest tests/ --verbose
